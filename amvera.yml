meta:
  environment: docker
  toolchain:
    name: pip
build:
  dockerfile: Dockerfile
  skip: false
run:
  image: null
  command: null
  args: null
  persistenceMount: /data
  containerPort: "80"
  servicePort: "80"
