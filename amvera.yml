meta:
  environment: python
  toolchain:
    name: pip
    version: "3.13"

build:
  requirementsPath: requirements.txt
  useCache: false

run:
  command: uvicorn main:app --host 0.0.0.0 --port 80
  persistenceMount: /data
  containerPort: 80

