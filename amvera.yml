meta:
  environment: docker
  toolchain:
    name: pip
build:
  dockerfile: Dockerfile
  skip: false
run:
  image: null
  command: uvicorn main:app --host 0.0.0.0 --port 80
  args: null
  persistenceMount: /data
  containerPort: "80"
  servicePort: "80"
